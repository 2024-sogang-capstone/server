spring:
    datasource:
        url: jdbc:mysql://${DB_HOST}:${DB_PORT}/emotion?characterEncoding=UTF-8
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
